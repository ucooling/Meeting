<%= form_for(@d_department) do |f| %>
  <% if @d_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@d_department.errors.count, "error") %> prohibited this d_department from being saved:</h2>

      <ul>
      <% @d_department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.number_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
